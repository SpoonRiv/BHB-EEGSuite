.DS_Store
Thumbs.db
Desktop.ini

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Virtual environments
.venv/
venv/
ENV/
env/

# Build artifacts
build/
dist/
*.egg-info/

# Logs & debug artifacts
*.log
logs/
log/
*.dmp
.dbg/
debug-*.md

# Runtime / exported data (avoid committing large outputs)
*.csv
*.tsv
*.edf
*.fif
*.mat
*.npz
*.npy
offlinedata/
raw_float32.bin

# IDE
.idea/
.vscode/
.qoder/
.codex_tmp/
.uploads/
.workbuddy/

# Local reference & project rules (runtime should not depend on these)
ref/

# Node (if web build tooling is introduced)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env files
.env
.env.*

# Local test & release tooling (kept only on this machine)
checks/

# Local runtime overrides (do not commit)
configs/config.local.yaml

# Optional modules are installed locally, never enabled by a fresh checkout.
module_data/

# VSCodeCounter (do not commit)
.VSCodeCounter/
